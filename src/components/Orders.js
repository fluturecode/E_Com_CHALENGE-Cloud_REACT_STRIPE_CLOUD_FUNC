import React from "react";
import "./Orders.css";

function Orders() {
	return (
		<div className="orders">
			<h1>Your Orders</h1>

			{/* <div className='orders__order'>
            {orders?.map(order => (
                <Order order={order} />
            ))}
        </div> */}
		</div>
	);
}

export default Orders;
